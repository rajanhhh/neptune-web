import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import tileCode from '../../../liveEditorCode/tile.code';
import pickerCode from '../../../liveEditorCode/picker.code';

import { Tile, Picker } from '@transferwise/components';

<LiveEditorBlock code={tileCode} scope={{ Tile }} />
<GeneratePropsTable componentName="Tile" />

## Usage
TODO: do we need to add information that this is used for major decisions where more context is needed?
E.g. deciding between two options

## Groups of tiles

TODO: Add information about the picker

TODO: Add notice for not showing more than 2-3 tiles for decision

<LiveEditorBlock code={pickerCode} scope={{ Picker }} display="vertical" />
<GeneratePropsTable componentName="Picker" />


export const meta = {
  name: 'Tile',
}