import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import code from '../../../liveEditorCode/tile.code';

import { Tile } from '@transferwise/components';

<LiveEditorBlock code={code} scope={{ Tile }} />
<GeneratePropsTable componentName="Tile" />

## Groups of tiles

It's recommended to use [`Picker`](https://transferwise.github.io/neptune-web/components/content/Picker) for displaying groups of tiles.

export const meta = {
  name: 'Tile',
}