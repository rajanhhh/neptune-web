import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import code from '../../../liveEditorCode/picker.code';

import { Picker, Tile, Avatar } from '@transferwise/components';

<LiveEditorBlock code={code} scope={{ Picker, Avatar }} display="vertical" />
<GeneratePropsTable componentName="Picker" />

## Usage
Picker can be used for illustrating major decisions for the user. 
Depending on the parent element width, the `items` are displayed using either the `Tile` or `NavigationOption` components.

It is encouraged to keep the number of options in `items` under three.

export const meta = {
  name: 'Picker',
}